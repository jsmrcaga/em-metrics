class Projects {
	constructor(projects) {
		this.id = id;
		this.integrations = {
			linear_id: ''
		};

		const { projects_by_user } = Object.entries(teams).reduce((agg, [team_name, { projects=[], users=[] }]) => {

		}, {projects_by_user: {}});

		this.projects_by_user = {};
	}

	static from_teams(teams={}) {

	}
}

module.exports = {
	Projects
};
